rules:
  artipacked:
    ignore:
      - github-statistics.yaml
      - latest-publications.yaml

  unpinned-uses:
    config:
      policies:
        actions/*: ref-pin

  secrets-outside-env:
    config:
      allow:
        - STATISTICS_TOKEN
